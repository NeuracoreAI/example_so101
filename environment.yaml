# SO101 teleop environment.
# No longer requires lerobot. The STS3215 motor driver uses feetech-servo-sdk (import scservo_sdk).
name: so101-teleop
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - numpy
      - scipy
      - opencv-python
      - viser
      - yourdfpy
      - pyserial
      - git+https://github.com/NeuracoreAI/neuracore.git@main
      - feetech-servo-sdk>=1.0.0,<2.0.0
